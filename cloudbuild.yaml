steps:
- name: gcr.io/cloud-builders/gcloud
  entrypoint: 'bash'
  id: 'push_tag'
  args:
  - '-c'
  - |
    pip install requests
    echo v`date +"%Y%m%d_%H%M%S"` > tag
    tag=`cat tag`
    python bin/create_pr.py $_GITHUB_API_TOKEN