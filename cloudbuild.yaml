steps:
- name: gcr.io/cloud-builders/gsutil
  entrypoint: 'bash'
  id: 'cache_dep_upload'
  args:
  - '-c'
  - |
    echo ${PROJECT_ID}
    echo ${_SUB_VALUE}
    echo ${COMMIT_SHA}
substitutions:
    _SUB_VALUE: world